/*  Program 5.14: Create a class “Person” with a method “speak()” that displays a message “The person is speaking”. 
Create two subclasses “Student” and “Teacher” that extend “Person” and implement the “speak()” method to display “The student is asking a question” and “The teacher is giving a lecture” respectively.
*/

class Person {
    void speak() {
        System.out.println("The person is speaking");
    }
}

class Student extends Person {
    @Override
    void speak() {
        System.out.println("The student is asking a question");
    }
}

class Teacher extends Person {
    @Override
    void speak() {
        System.out.println("The teacher is giving a lecture");
    }
}

public class Main {
    public static void main(String[] args) {
        // Object of Person
        Person p = new Person();
        p.speak();  // prints "The person is speaking"

        // Object of Student
        Student s = new Student();
        s.speak();  // prints "The student is asking a question"

        // Object of Teacher
        Teacher t = new Teacher();
        t.speak();  // prints "The teacher is giving a lecture"
    }
}
